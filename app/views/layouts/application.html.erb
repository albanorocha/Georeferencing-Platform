<!DOCTYPE html>
<html>
<head>
  <title>IMedia</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= favicon_link_tag "fav.ico", type: 'image/vnd.microsoft.icon'%>
  <%= csrf_meta_tags %>
  
  	<!-- script references -->
		<script src="//ajax.googleapis.com/ajax/libs/jquery/2.0.2/jquery.min.js"></script>
		<script src="assets/javascritps/bootstrap.min.js"></script>
</head>
<body>
 
<!-- begin template -->
<div class="navbar navbar-custom navbar-fixed-top">

  <div class="navbar-header">   
<%= link_to(root_path) do %>
   <%= image_tag("logo-site.fw.png",  :class => "") if @logo.nil? %>
  <%= image_tag(@logo.image.url(:logo),  :class => "") unless @logo.nil? %>
<% end %>
   
      <a class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="active"><a href="#">Sobre</a></li>
        <li><a href="#">Contato</a></li>

        <li>&nbsp;</li>
      </ul>
      <form class="navbar-form">
        <div class="form-group" style="display:inline;">
          <div class="input-group">
            <div class="input-group-btn">
              <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-chevron-down"></span></button>
              <ul class="dropdown-menu">
                <li><a href="#">Outdoor</a></li>
                <li><a href="#">Busdoor</a></li>
                <li><a href="#">Indoor</a></li>
              </ul>
            </div>
            <input type="text" class="form-control" placeholder="O que você procura?">
            <span class="input-group-addon"><span class="glyphicon glyphicon-search"></span> </span>
          </div>
        </div>
      </form>
    </div>
</div>  
   <h1> iMedia </h1>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <ul>
  <li> <%= link_to "Home", root_path %> </li>
  <li> <%= link_to "Outdoor", outdoors_path %> </li>
  <% if user_signed_in? %>
  	<p> Logged in as <strong><%= current_user.email %></strong>. </p>
    <li> <%= link_to "Logout", destroy_user_session_path, method: :delete %> </li>
  <% else %>
    <li> <%= link_to "Login", new_user_session_path  %> </li>
  <% end %>
  </ul>


<%= yield %>

  
</body>
</html>
