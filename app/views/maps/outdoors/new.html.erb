<h1>New outdoor</h1>

<%= render partial: 'form', locals: {external_url: dashboard_outdoors_path} %>

<%= link_to 'Back', dashboard_outdoors_path %>


<%= javascript_tag do %>
  buildNewMap(<%= raw  @hash.to_json %>);
<% end %>